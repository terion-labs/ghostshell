#!/bin/sh
set -eu

if [ "${1-}" = "--sdk" ] && [ "${2-}" = "macosx" ] && [ "${3-}" = "--show-sdk-path" ]; then
    if [ -n "${ASURA_MACOS_SDK-}" ]; then
        printf '%s\n' "$ASURA_MACOS_SDK"
        exit 0
    fi
fi

exec /usr/bin/xcrun "$@"
