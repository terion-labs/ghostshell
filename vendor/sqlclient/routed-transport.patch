--- /dev/null
+++ b/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft.Data.SqlClient.Routed.csproj
@@ -0,0 +1,50 @@
+<Project Sdk="Microsoft.NET.Sdk">
+  <PropertyGroup>
+    <SqlClientVendorRoot>$([MSBuild]::NormalizeDirectory('$(MSBuildThisFileDirectory)../../../../..'))</SqlClientVendorRoot>
+    <TargetFramework>net9.0</TargetFramework>
+    <AssemblyName>Microsoft.Data.SqlClient</AssemblyName>
+    <PackageId>Microsoft.Data.SqlClient.Routed</PackageId>
+    <Version>6.0.2</Version>
+    <AssemblyVersion>6.0.0.0</AssemblyVersion>
+    <FileVersion>6.0.2.0</FileVersion>
+    <SignAssembly>true</SignAssembly>
+    <PublicSign>true</PublicSign>
+    <AssemblyOriginatorKeyFile>$(SqlClientVendorRoot)sqlclient-public.snk</AssemblyOriginatorKeyFile>
+    <LangVersion>9.0</LangVersion>
+    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
+    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
+    <EnableDefaultEmbeddedResourceItems>false</EnableDefaultEmbeddedResourceItems>
+    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
+    <Deterministic>true</Deterministic>
+    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
+    <EnableTrimAnalyzer>true</EnableTrimAnalyzer>
+    <!-- Unchanged upstream source-boundary trim diagnostics. -->
+    <NoWarn>$(NoWarn);IL2026;IL2057;IL2072;IL2075</NoWarn>
+    <GenerateDocumentationFile>true</GenerateDocumentationFile>
+    <RootNamespace />
+    <OSGroup Condition="'$(RuntimeIdentifier)' != '' and $([System.String]::Copy('$(RuntimeIdentifier)').StartsWith('win-'))">Windows_NT</OSGroup>
+    <OSGroup Condition="'$(RuntimeIdentifier)' != '' and !$([System.String]::Copy('$(RuntimeIdentifier)').StartsWith('win-'))">Unix</OSGroup>
+    <OSGroup Condition="'$(OSGroup)' == ''">$(OS)</OSGroup>
+    <TargetsWindows Condition="'$(OSGroup)' == 'Windows_NT'">true</TargetsWindows>
+    <TargetsUnix Condition="'$(OSGroup)' == 'Unix'">true</TargetsUnix>
+    <CommonSourceRoot>$(SqlClientVendorRoot)upstream/src/Microsoft.Data.SqlClient/src/</CommonSourceRoot>
+    <SqlClientNetCoreRoot>$(MSBuildThisFileDirectory)</SqlClientNetCoreRoot>
+    <CommonPath>$(SqlClientNetCoreRoot)Common/src</CommonPath>
+    <NetCoreSource>$(SqlClientVendorRoot)upstream/src/Microsoft.Data.SqlClient/netcore/</NetCoreSource>
+    <ResxFileName>Strings</ResxFileName>
+  </PropertyGroup>
+  <Import Project="$(SqlClientVendorRoot)Upstream.Items.props" />
+  <ItemGroup>
+    <Compile Include="$(SqlClientVendorRoot)ThisAssembly.cs" />
+    <Compile Include="$(SqlClientVendorRoot)Strings.ResourceNames.cs" />
+    <PackageReference Include="Microsoft.Data.SqlClient.SNI.runtime" />
+    <PackageReference Include="Azure.Identity" />
+    <PackageReference Include="Microsoft.Extensions.Caching.Memory" />
+    <PackageReference Include="Microsoft.IdentityModel.JsonWebTokens" />
+    <PackageReference Include="Microsoft.IdentityModel.Protocols.OpenIdConnect" />
+    <PackageReference Include="Microsoft.SqlServer.Server" />
+    <PackageReference Include="System.Configuration.ConfigurationManager" />
+    <PackageReference Include="System.Security.Cryptography.Pkcs" />
+    <PackageReference Include="Microsoft.Bcl.Cryptography" />
+  </ItemGroup>
+</Project>
--- a/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft.Data.SqlClient.csproj
+++ b/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft.Data.SqlClient.csproj
@@ -37,6 +37,9 @@
     <EmbeddedFiles Include="$(TargetFrameworkMonikerAssemblyAttributesPath)" />
   </ItemGroup>  
   <!-- Contains common items shared between Unix and Windows -->
+  <ItemGroup>
+    <Compile Include="$(CommonSourceRoot)Microsoft\Data\SqlClient\SqlConnectionTcpTransport.cs" />
+  </ItemGroup>
   <ItemGroup Condition="'$(OSGroup)' != 'AnyOS'">
     <Compile Include="$(CommonSourceRoot)\Interop\Common\Sni\SniErrors.cs">
       <Link>Interop\Common\Sni\SniErrors.cs</Link>
--- a/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/SNI/SNIProxy.cs
+++ b/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/SNI/SNIProxy.cs
@@ -46,6 +46,7 @@
         /// <param name="hostNameInCertificate">Used for the HostName in certificate</param>
         /// <param name="serverCertificateFilename">Used for the path to the Server Certificate</param>
         /// <returns>SNI handle</returns>
+        /// <param name="tcpTransport">Optional route for every physical TCP connection.</param>
         internal static SNIHandle CreateConnectionHandle(
             string fullServerName,
             TimeoutTimer timeout,
@@ -61,13 +62,18 @@
             ref SQLDNSInfo pendingDNSInfo,
             bool tlsFirst,
             string hostNameInCertificate,
-            string serverCertificateFilename)
+            string serverCertificateFilename,
+            SqlConnectionTcpTransport tcpTransport = null)
         {
             instanceName = new byte[1];
+            tcpTransport?.Lifetime.ThrowIfCancellationRequested();
+            if (tcpTransport != null && fullServerName.TrimStart().StartsWith("(localdb)", StringComparison.OrdinalIgnoreCase))
+            {
+                throw new NotSupportedException("Routed connections do not support LocalDB.");
+            }
+            bool errorWithLocalDBProcessing = false;
+            string localDBDataSource = tcpTransport == null ? GetLocalDBDataSource(fullServerName, out errorWithLocalDBProcessing) : null;
 
-            bool errorWithLocalDBProcessing;
-            string localDBDataSource = GetLocalDBDataSource(fullServerName, out errorWithLocalDBProcessing);
-
             if (errorWithLocalDBProcessing)
             {
                 return null;
@@ -81,6 +87,11 @@
                 return null;
             }
 
+            if (tcpTransport != null && details.ResolvedProtocol == DataSource.Protocol.NP)
+            {
+                throw new NotSupportedException("Routed connections require TCP.");
+            }
+
             SNIHandle sniHandle = null;
             switch (details.ResolvedProtocol)
             {
@@ -88,7 +99,7 @@
                 case DataSource.Protocol.None: // default to using tcp if no protocol is provided
                 case DataSource.Protocol.TCP:
                     sniHandle = CreateTcpHandle(details, timeout, parallel, ipPreference, cachedFQDN, ref pendingDNSInfo,
-                        tlsFirst, hostNameInCertificate, serverCertificateFilename);
+                        tlsFirst, hostNameInCertificate, serverCertificateFilename, tcpTransport);
                     break;
                 case DataSource.Protocol.NP:
                     sniHandle = CreateNpHandle(details, timeout, parallel, tlsFirst, hostNameInCertificate, serverCertificateFilename);
@@ -102,7 +113,7 @@
             {
                 try
                 {
-                    spnBuffer = GetSqlServerSPNs(details, serverSPN);
+                    spnBuffer = GetSqlServerSPNs(details, serverSPN, tcpTransport == null);
                 }
                 catch (Exception e)
                 {
@@ -114,7 +125,7 @@
             return sniHandle;
         }
 
-        private static byte[][] GetSqlServerSPNs(DataSource dataSource, string serverSPN)
+        private static byte[][] GetSqlServerSPNs(DataSource dataSource, string serverSPN, bool resolveHostName = true)
         {
             Debug.Assert(!string.IsNullOrWhiteSpace(dataSource.ServerName));
             if (!string.IsNullOrWhiteSpace(serverSPN))
@@ -134,17 +145,20 @@
             }
 
             SqlClientEventSource.Log.TryTraceEvent("SNIProxy.GetSqlServerSPN | Info | ServerName {0}, InstanceName {1}, Port {2}, postfix {3}", dataSource?.ServerName, dataSource?.InstanceName, dataSource?.Port, postfix);
-            return GetSqlServerSPNs(hostName, postfix, dataSource.ResolvedProtocol);
+            return GetSqlServerSPNs(hostName, postfix, dataSource.ResolvedProtocol, resolveHostName);
         }
 
-        private static byte[][] GetSqlServerSPNs(string hostNameOrAddress, string portOrInstanceName, DataSource.Protocol protocol)
+        private static byte[][] GetSqlServerSPNs(string hostNameOrAddress, string portOrInstanceName, DataSource.Protocol protocol, bool resolveHostName = true)
         {
             Debug.Assert(!string.IsNullOrWhiteSpace(hostNameOrAddress));
             IPHostEntry hostEntry = null;
             string fullyQualifiedDomainName;
             try
             {
-                hostEntry = Dns.GetHostEntry(hostNameOrAddress);
+                if (resolveHostName)
+                {
+                    hostEntry = Dns.GetHostEntry(hostNameOrAddress);
+                }
             }
             catch (SocketException)
             {
@@ -189,6 +203,7 @@
         /// <param name="hostNameInCertificate">Host name in certificate</param>
         /// <param name="serverCertificateFilename">Used for the path to the Server Certificate</param>
         /// <returns>SNITCPHandle</returns>
+        /// <param name="tcpTransport">Optional route for every physical TCP connection.</param>
         private static SNITCPHandle CreateTcpHandle(
             DataSource details,
             TimeoutTimer timeout,
@@ -198,7 +213,8 @@
             ref SQLDNSInfo pendingDNSInfo,
             bool tlsFirst,
             string hostNameInCertificate,
-            string serverCertificateFilename)
+            string serverCertificateFilename,
+            SqlConnectionTcpTransport tcpTransport = null)
         {
             // TCP Format:
             // tcp:<host name>\<instance name>
@@ -215,6 +231,10 @@
             bool isAdminConnection = details.ResolvedProtocol == DataSource.Protocol.Admin;
             if (details.IsSsrpRequired)
             {
+                if (tcpTransport != null)
+                {
+                    throw new NotSupportedException("Routed named instances require an explicit TCP port.");
+                }
                 try
                 {
                     details.ResolvedPort = port = isAdminConnection ?
@@ -237,7 +257,7 @@
             }
 
             return new SNITCPHandle(hostName, port, timeout, parallel, ipPreference, cachedFQDN, ref pendingDNSInfo,
-                tlsFirst, hostNameInCertificate, serverCertificateFilename);
+                tlsFirst, hostNameInCertificate, serverCertificateFilename, tcpTransport);
         }
 
         /// <summary>
--- a/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/SNI/SNITcpHandle.cs
+++ b/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/SNI/SNITcpHandle.cs
@@ -42,12 +42,14 @@
         private Guid _connectionId = Guid.NewGuid();
 
         private const int MaxParallelIpAddresses = 64;
+        private System.Threading.CancellationTokenRegistration _routeCancellation;
 
         /// <summary>
         /// Dispose object
         /// </summary>
         public override void Dispose()
         {
+            _routeCancellation.Dispose();
             lock (this)
             {
                 if (_sslOverTdsStream != null)
@@ -124,6 +126,7 @@
         /// <param name="tlsFirst">Support TDS8.0</param>
         /// <param name="hostNameInCertificate">Host Name in Certificate</param>
         /// <param name="serverCertificateFilename">Used for the path to the Server Certificate</param>
+        /// <param name="tcpTransport">Optional route for every physical TCP connection.</param>
         public SNITCPHandle(
             string serverName,
             int port,
@@ -134,7 +137,8 @@
             ref SQLDNSInfo pendingDNSInfo,
             bool tlsFirst,
             string hostNameInCertificate,
-            string serverCertificateFilename)
+            string serverCertificateFilename,
+            SqlConnectionTcpTransport tcpTransport = null)
         {
             using (TrySNIEventScope.Create(nameof(SNITCPHandle)))
             {
@@ -159,8 +163,13 @@
                     // Exceptions will be thrown to the caller and be handled as before.
                     try
                     {
-                        if (parallel)
+                        if (tcpTransport != null)
                         {
+                            _socket = tcpTransport.Connect(serverName, port, parallel, ipPreference, timeout);
+                            _routeCancellation = tcpTransport.Lifetime.Register(static state => ((Socket)state).Dispose(), _socket);
+                        }
+                        else if (parallel)
+                        {
                             _socket = TryConnectParallel(serverName, port, timeout, ref reportError, cachedFQDN, ref pendingDNSInfo);
                         }
                         else
@@ -170,7 +179,7 @@
                     }
                     catch (Exception ex)
                     {
-                        if (timeout.IsExpired)
+                        if (tcpTransport != null || timeout.IsExpired)
                         {
                             throw;
                         }
--- a/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/SqlConnection.cs
+++ b/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/SqlConnection.cs
@@ -213,6 +213,24 @@
             // else
             //      credential == null:  we should not set "Credential" as this will do additional validation check and
             //      checking pool groups which is not necessary. All necessary operation is already done by calling "ConnectionString = connectionString"
+        }
+
+        private SqlConnectionTcpTransport _tcpTransport;
+
+        /// <summary>Optional physical TCP route. Its identity isolates connection pools.</summary>
+        public SqlConnectionTcpTransport TcpTransport
+        {
+            get => _tcpTransport;
+            set
+            {
+                if (!InnerConnection.AllowSetConnectionString)
+                {
+                    throw ADP.OpenConnectionPropertySet(nameof(TcpTransport), InnerConnection.State);
+                }
+                value?.Lifetime.ThrowIfCancellationRequested();
+                ConnectionString_Set(new SqlConnectionPoolKey(_connectionString, _credential, _accessToken, _accessTokenCallback, value));
+                _tcpTransport = value;
+            }
         }
 
         private SqlConnection(SqlConnection connection)
@@ -229,6 +247,7 @@
 
             _accessToken = connection._accessToken;
             _accessTokenCallback = connection._accessTokenCallback;
+            _tcpTransport = connection._tcpTransport;
             CacheConnectionStringProperties();
         }
 
@@ -648,7 +667,7 @@
                         CheckAndThrowOnInvalidCombinationOfConnectionOptionAndAccessTokenCallback(connectionOptions);
                     }
                 }
-                ConnectionString_Set(new SqlConnectionPoolKey(value, _credential, _accessToken, _accessTokenCallback));
+                ConnectionString_Set(new SqlConnectionPoolKey(value, _credential, _accessToken, _accessTokenCallback, _tcpTransport));
                 _connectionString = value;  // Change _connectionString value only after value is validated
                 CacheConnectionStringProperties();
             }
@@ -708,7 +727,7 @@
                 }
 
                 // Need to call ConnectionString_Set to do proper pool group check
-                ConnectionString_Set(new SqlConnectionPoolKey(_connectionString, credential: _credential, accessToken: value, accessTokenCallback: null));
+                ConnectionString_Set(new SqlConnectionPoolKey(_connectionString, credential: _credential, accessToken: value, accessTokenCallback: null, tcpTransport: _tcpTransport));
                 _accessToken = value;
             }
         }
@@ -731,7 +750,7 @@
                     CheckAndThrowOnInvalidCombinationOfConnectionOptionAndAccessTokenCallback((SqlConnectionString)ConnectionOptions);
                 }
 
-                ConnectionString_Set(new SqlConnectionPoolKey(_connectionString, credential: _credential, accessToken: null, accessTokenCallback: value));
+                ConnectionString_Set(new SqlConnectionPoolKey(_connectionString, credential: _credential, accessToken: null, accessTokenCallback: value, tcpTransport: _tcpTransport));
                 _accessTokenCallback = value;
             }
         }
@@ -1030,7 +1049,7 @@
                 _credential = value;
 
                 // Need to call ConnectionString_Set to do proper pool group check
-                ConnectionString_Set(new SqlConnectionPoolKey(_connectionString, _credential, accessToken: _accessToken, accessTokenCallback: _accessTokenCallback));
+                ConnectionString_Set(new SqlConnectionPoolKey(_connectionString, _credential, accessToken: _accessToken, accessTokenCallback: _accessTokenCallback, tcpTransport: _tcpTransport));
             }
         }
 
@@ -1900,6 +1919,7 @@
 
         private bool TryOpen(TaskCompletionSource<DbConnectionInternal> retry, SqlConnectionOverrides overrides = SqlConnectionOverrides.None)
         {
+            _tcpTransport?.Lifetime.ThrowIfCancellationRequested();
             SqlConnectionString connectionOptions = (SqlConnectionString)ConnectionOptions;
 
             if (_cultureCheckState != CultureCheckState.Standard)
@@ -1974,6 +1994,12 @@
             }
             // does not require GC.KeepAlive(this) because of ReRegisterForFinalize below.
 
+            if (_tcpTransport?.Lifetime.IsCancellationRequested == true)
+            {
+                CloseInnerConnection();
+                _tcpTransport.Lifetime.ThrowIfCancellationRequested();
+            }
+
             var tdsInnerConnection = (SqlInternalConnectionTds)InnerConnection;
 
             Debug.Assert(tdsInnerConnection.Parser != null, "Where's the parser?");
--- a/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/SqlConnectionFactory.cs
+++ b/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/SqlConnectionFactory.cs
@@ -40,6 +40,11 @@
         {
             SqlConnectionString opt = (SqlConnectionString)options;
             SqlConnectionPoolKey key = (SqlConnectionPoolKey)poolKey;
+            key.TcpTransport?.Lifetime.ThrowIfCancellationRequested();
+            if (key.TcpTransport != null && opt.UserInstance)
+            {
+                throw new NotSupportedException("Routed connections do not support local user instances.");
+            }
             SessionData recoverySessionData = null;
 
             SqlConnection sqlOwningConnection = (SqlConnection)owningConnection;
@@ -135,7 +140,7 @@
                 opt = new SqlConnectionString(opt, instanceName, userInstance: false, setEnlistValue: null);
                 poolGroupProviderInfo = null; // null so we do not pass to constructor below...
             }
-            return new SqlInternalConnectionTds(identity, opt, key.Credential, poolGroupProviderInfo, "", null, redirectedUserInstance, userOpt, recoverySessionData, applyTransientFaultHandling: applyTransientFaultHandling, key.AccessToken, pool, key.AccessTokenCallback);
+            return new SqlInternalConnectionTds(identity, opt, key.Credential, poolGroupProviderInfo, "", null, redirectedUserInstance, userOpt, recoverySessionData, applyTransientFaultHandling: applyTransientFaultHandling, key.AccessToken, pool, key.AccessTokenCallback, key.TcpTransport);
         }
 
         protected override DbConnectionOptions CreateConnectionOptions(string connectionString, DbConnectionOptions previous)
@@ -327,4 +332,3 @@
         partial void SubscribeToAssemblyLoadContextUnload();
     }
 }
-
--- a/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/SqlInternalConnectionTds.cs
+++ b/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/SqlInternalConnectionTds.cs
@@ -436,6 +436,7 @@
         private Guid _originalClientConnectionId = Guid.Empty;
         private string _routingDestination = null;
         private readonly TimeoutTimer _timeout;
+        private readonly SqlConnectionTcpTransport _tcpTransport;
 
         // although the new password is generally not used it must be passed to the ctor
         // the new Login7 packet will always write out the new password (or a length of zero and no bytes if not present)
@@ -454,7 +455,8 @@
             string accessToken = null,
             DbConnectionPool pool = null,
             Func<SqlAuthenticationParameters, CancellationToken,
-            Task<SqlAuthenticationToken>> accessTokenCallback = null) : base(connectionOptions)
+            Task<SqlAuthenticationToken>> accessTokenCallback = null,
+            SqlConnectionTcpTransport tcpTransport = null) : base(connectionOptions)
 
         {
 #if DEBUG
@@ -466,6 +468,8 @@
             Debug.Assert(reconnectSessionData == null || connectionOptions.ConnectRetryCount > 0, "Reconnect data supplied with CR turned off");
 
             _dbConnectionPool = pool;
+            _tcpTransport = tcpTransport;
+            _tcpTransport?.Lifetime.ThrowIfCancellationRequested();
 
             if (connectionOptions.ConnectRetryCount > 0)
             {
@@ -1564,7 +1568,7 @@
                 if (_parser != null)
                     _parser.Disconnect();
 
-                _parser = new TdsParser(ConnectionOptions.MARS, ConnectionOptions.Asynchronous);
+                _parser = new TdsParser(ConnectionOptions.MARS, ConnectionOptions.Asynchronous, _tcpTransport);
                 Debug.Assert(SniContext.Undefined == Parser._physicalStateObj.SniContext, $"SniContext should be Undefined; actual Value: {Parser._physicalStateObj.SniContext}");
 
                 try
@@ -1785,7 +1789,7 @@
                     _parser.Disconnect();
                 }
 
-                _parser = new TdsParser(ConnectionOptions.MARS, ConnectionOptions.Asynchronous);
+                _parser = new TdsParser(ConnectionOptions.MARS, ConnectionOptions.Asynchronous, _tcpTransport);
                 Debug.Assert(SniContext.Undefined == Parser._physicalStateObj.SniContext, $"SniContext should be Undefined; actual Value: {Parser._physicalStateObj.SniContext}");
 
                 ServerInfo currentServerInfo;
@@ -1832,7 +1836,7 @@
                             _parser.Disconnect();
                         }
 
-                        _parser = new TdsParser(ConnectionOptions.MARS, connectionOptions.Asynchronous);
+                        _parser = new TdsParser(ConnectionOptions.MARS, connectionOptions.Asynchronous, _tcpTransport);
 
                         Debug.Assert(SniContext.Undefined == Parser._physicalStateObj.SniContext, $"SniContext should be Undefined; actual Value: {Parser._physicalStateObj.SniContext}");
 
--- a/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/TdsParser.Windows.cs
+++ b/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/TdsParser.Windows.cs
@@ -13,7 +13,7 @@
     {
         internal void PostReadAsyncForMars()
         {
-            if (TdsParserStateObjectFactory.UseManagedSNI)
+            if (_stateObjectFactory.UsesManagedSNI)
                 return;
 
             // HACK HACK HACK - for Async only
@@ -62,7 +62,7 @@
         {
             SNIErrorDetails details = new SNIErrorDetails();
 
-            if (TdsParserStateObjectFactory.UseManagedSNI)
+            if (_stateObjectFactory.UsesManagedSNI)
             {
                 SNIError sniError = SNIProxy.Instance.GetLastError();
                 details.sniErrorNumber = sniError.sniError;
--- a/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/TdsParser.cs
+++ b/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/TdsParser.cs
@@ -74,7 +74,7 @@
         /// <summary>
         /// Verify client encryption possibility.
         /// </summary>
-        private bool ClientOSEncryptionSupport => TdsParserStateObjectFactory.Singleton.ClientOSEncryptionSupport;
+        private bool ClientOSEncryptionSupport => _stateObjectFactory.ClientOSEncryptionSupport;
 
         // Default state object for parser
         internal TdsParserStateObject _physicalStateObj = null; // Default stateObj and connection for Dbnetlib and non-MARS SNI.
@@ -109,10 +109,10 @@
 
         internal Encoding _defaultEncoding = null;                  // for sql character data
 
-        private static EncryptionOptions s_sniSupportedEncryptionOption = TdsParserStateObjectFactory.Singleton.EncryptionOptions;
+        private EncryptionOptions _encryptionOption;
+        private readonly TdsParserStateObjectFactory _stateObjectFactory;
+        internal SqlConnectionTcpTransport TcpTransport { get; }
 
-        private EncryptionOptions _encryptionOption = s_sniSupportedEncryptionOption;
-
         private SqlInternalTransaction _currentTransaction;
         private SqlInternalTransaction _pendingTransaction;    // pending transaction for 2005 and beyond.
         // SQLHOT 483
@@ -215,11 +215,14 @@
 
         private SqlCollation _cachedCollation;
 
-        internal TdsParser(bool MARS, bool fAsynchronous)
+        internal TdsParser(bool MARS, bool fAsynchronous, SqlConnectionTcpTransport tcpTransport = null)
         {
             _fMARS = MARS; // may change during Connect to pre 2005 servers
-
-            _physicalStateObj = TdsParserStateObjectFactory.Singleton.CreateTdsParserStateObject(this);
+            TcpTransport = tcpTransport;
+            TcpTransport?.Lifetime.ThrowIfCancellationRequested();
+            _stateObjectFactory = tcpTransport == null ? TdsParserStateObjectFactory.Singleton : TdsParserStateObjectFactory.ManagedSingleton;
+            _encryptionOption = _stateObjectFactory.EncryptionOptions;
+            _physicalStateObj = _stateObjectFactory.CreateTdsParserStateObject(this);
             DataClassificationVersion = TdsEnums.DATA_CLASSIFICATION_NOT_ENABLED;
         }
 
@@ -412,7 +415,7 @@
             // Clean up IsSQLDNSCachingSupported flag from previous status
             _connHandler.IsSQLDNSCachingSupported = false;
 
-            uint sniStatus = TdsParserStateObjectFactory.Singleton.SNIStatus;
+            uint sniStatus = _stateObjectFactory.SNIStatus;
 
             if (sniStatus != TdsEnums.SNI_SUCCESS)
             {
@@ -661,7 +664,7 @@
                 // Cache physical stateObj and connection.
                 _pMarsPhysicalConObj = _physicalStateObj;
 
-                if (TdsParserStateObjectFactory.UseManagedSNI)
+                if (_stateObjectFactory.UsesManagedSNI)
                     _pMarsPhysicalConObj.IncrementPendingCallbacks();
 
                 uint info = 0;
@@ -681,7 +684,7 @@
 
         internal TdsParserStateObject CreateSession()
         {
-            TdsParserStateObject session = TdsParserStateObjectFactory.Singleton.CreateSessionObject(this, _pMarsPhysicalConObj, true);
+            TdsParserStateObject session = _stateObjectFactory.CreateSessionObject(this, _pMarsPhysicalConObj, true);
             SqlClientEventSource.Log.TryAdvancedTraceEvent("<sc.TdsParser.CreateSession|ADV> {0} created session {1}", ObjectID, session.ObjectID);
             return session;
         }
@@ -1526,7 +1529,7 @@
                  * !=null       | == 0     | replace text left of errorMessage
                  */
 
-                if (TdsParserStateObjectFactory.UseManagedSNI)
+                if (_stateObjectFactory.UsesManagedSNI)
                 {
                     Debug.Assert(!string.IsNullOrEmpty(details.errorMessage) || details.sniErrorNumber != 0, "Empty error message received from SNI");
                     SqlClientEventSource.Log.TryAdvancedTraceEvent("<sc.TdsParser.ProcessSNIError |ERR|ADV > Empty error message received from SNI. Error Message = {0}, SNI Error Number ={1}", details.errorMessage, details.sniErrorNumber);
@@ -1574,7 +1577,7 @@
                 }
                 else
                 {
-                    if (TdsParserStateObjectFactory.UseManagedSNI)
+                    if (_stateObjectFactory.UsesManagedSNI)
                     {
                         // SNI error. Append additional error message info if available and hasn't been included.
                         string sniLookupMessage = SQL.GetSNIErrorMessage(details.sniErrorNumber);
--- a/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/TdsParserStateObjectFactory.Managed.cs
+++ b/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/TdsParserStateObjectFactory.Managed.cs
@@ -12,6 +12,8 @@
         public static bool UseManagedSNI => true;
 
         public static readonly TdsParserStateObjectFactory Singleton = new TdsParserStateObjectFactory();
+        public static readonly TdsParserStateObjectFactory ManagedSingleton = Singleton;
+        public bool UsesManagedSNI => true;
 
         public EncryptionOptions EncryptionOptions => SNI.SNILoadHandle.SingletonInstance.Options;
 
--- a/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/TdsParserStateObjectFactory.Windows.cs
+++ b/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/TdsParserStateObjectFactory.Windows.cs
@@ -10,6 +10,10 @@
     internal sealed class TdsParserStateObjectFactory
     {
         public static readonly TdsParserStateObjectFactory Singleton = new TdsParserStateObjectFactory();
+        public static readonly TdsParserStateObjectFactory ManagedSingleton = new TdsParserStateObjectFactory(true);
+        private readonly bool _forceManagedSni;
+        private TdsParserStateObjectFactory(bool forceManagedSni = false) => _forceManagedSni = forceManagedSni;
+        public bool UsesManagedSNI => _forceManagedSni || UseManagedSNI;
 
         private const string UseManagedNetworkingOnWindows = "Switch.Microsoft.Data.SqlClient.UseManagedNetworkingOnWindows";
 
@@ -19,18 +23,18 @@
         public static bool UseManagedSNI => 
             AppContext.TryGetSwitch(UseManagedNetworkingOnWindows, out shouldUseManagedSNI) ? shouldUseManagedSNI : false;
 
-        public EncryptionOptions EncryptionOptions => UseManagedSNI ? SNI.SNILoadHandle.SingletonInstance.Options : SNILoadHandle.SingletonInstance.Options;
+        public EncryptionOptions EncryptionOptions => UsesManagedSNI ? SNI.SNILoadHandle.SingletonInstance.Options : SNILoadHandle.SingletonInstance.Options;
 
-        public uint SNIStatus => UseManagedSNI ? SNI.SNILoadHandle.SingletonInstance.Status : SNILoadHandle.SingletonInstance.Status;
+        public uint SNIStatus => UsesManagedSNI ? SNI.SNILoadHandle.SingletonInstance.Status : SNILoadHandle.SingletonInstance.Status;
 
         /// <summary>
         /// Verify client encryption possibility.
         /// </summary>
-        public bool ClientOSEncryptionSupport => UseManagedSNI ? SNI.SNILoadHandle.SingletonInstance.ClientOSEncryptionSupport : SNILoadHandle.SingletonInstance.ClientOSEncryptionSupport;
+        public bool ClientOSEncryptionSupport => UsesManagedSNI ? SNI.SNILoadHandle.SingletonInstance.ClientOSEncryptionSupport : SNILoadHandle.SingletonInstance.ClientOSEncryptionSupport;
 
         public TdsParserStateObject CreateTdsParserStateObject(TdsParser parser)
         {
-            if (UseManagedSNI)
+            if (UsesManagedSNI)
             {
                 SqlClientEventSource.Log.TryTraceEvent("TdsParserStateObjectFactory.CreateTdsParserStateObject | Info | Found AppContext switch '{0}' enabled, managed networking implementation will be used."
                    , UseManagedNetworkingOnWindows);
@@ -46,7 +50,7 @@
 
         internal TdsParserStateObject CreateSessionObject(TdsParser tdsParser, TdsParserStateObject _pMarsPhysicalConObj, bool v)
         {
-            if (TdsParserStateObjectFactory.UseManagedSNI)
+            if (UsesManagedSNI)
             {
                 return new TdsParserStateObjectManaged(tdsParser, _pMarsPhysicalConObj, true);
             }
--- a/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/TdsParserStateObjectManaged.cs
+++ b/upstream/src/Microsoft.Data.SqlClient/netcore/src/Microsoft/Data/SqlClient/TdsParserStateObjectManaged.cs
@@ -96,7 +96,7 @@
         {
             SNIHandle? sessionHandle = SNIProxy.CreateConnectionHandle(serverName, timeout, out instanceName, ref spnBuffer, serverSPN,
                 flushCache, async, parallel, isIntegratedSecurity, iPAddressPreference, cachedFQDN, ref pendingDNSInfo, tlsFirst,
-                hostNameInCertificate, serverCertificateFilename);
+                hostNameInCertificate, serverCertificateFilename, _parser.TcpTransport);
 
             if (sessionHandle is not null)
             {
--- a/upstream/src/Microsoft.Data.SqlClient/src/Microsoft/Data/SqlClient/SqlConnectionPoolKey.cs
+++ b/upstream/src/Microsoft.Data.SqlClient/src/Microsoft/Data/SqlClient/SqlConnectionPoolKey.cs
@@ -21,6 +21,7 @@
 
         internal SqlCredential Credential => _credential;
         internal string AccessToken => _accessToken;
+        internal SqlConnectionTcpTransport TcpTransport { get; }
         internal Func<SqlAuthenticationParameters, CancellationToken, Task<SqlAuthenticationToken>> AccessTokenCallback => _accessTokenCallback;
 
         internal override string ConnectionString
@@ -33,12 +34,13 @@
             }
         }
 
-        internal SqlConnectionPoolKey(string connectionString, SqlCredential credential, string accessToken, Func<SqlAuthenticationParameters, CancellationToken, Task<SqlAuthenticationToken>> accessTokenCallback) : base(connectionString)
+        internal SqlConnectionPoolKey(string connectionString, SqlCredential credential, string accessToken, Func<SqlAuthenticationParameters, CancellationToken, Task<SqlAuthenticationToken>> accessTokenCallback, SqlConnectionTcpTransport tcpTransport = null) : base(connectionString)
         {
             Debug.Assert(credential == null || accessToken == null || accessTokenCallback == null, "Credential, AccessToken, and Callback can't have a value at the same time.");
             _credential = credential;
             _accessToken = accessToken;
             _accessTokenCallback = accessTokenCallback;
+            TcpTransport = tcpTransport;
             CalculateHashCode();
         }
 
@@ -47,6 +49,7 @@
             _credential = key.Credential;
             _accessToken = key.AccessToken;
             _accessTokenCallback = key._accessTokenCallback;
+            TcpTransport = key.TcpTransport;
             CalculateHashCode();
         }
 
@@ -59,6 +62,7 @@
         {
             return (obj is SqlConnectionPoolKey key
                 && _credential == key._credential
+                && ReferenceEquals(TcpTransport, key.TcpTransport)
                 && ConnectionString == key.ConnectionString
                 && _accessTokenCallback == key._accessTokenCallback
                 && string.CompareOrdinal(_accessToken, key._accessToken) == 0);
@@ -72,6 +76,10 @@
         private void CalculateHashCode()
         {
             _hashValue = base.GetHashCode();
+            if (TcpTransport != null)
+            {
+                unchecked { _hashValue = _hashValue * 17 + System.Runtime.CompilerServices.RuntimeHelpers.GetHashCode(TcpTransport); }
+            }
 
             if (_credential != null)
             {
--- a/upstream/src/Microsoft.Data.SqlClient/src/Microsoft/Data/SqlClient/SqlConnectionStringBuilder.cs
+++ b/upstream/src/Microsoft.Data.SqlClient/src/Microsoft/Data/SqlClient/SqlConnectionStringBuilder.cs
@@ -132,7 +132,7 @@
         private bool _transparentNetworkIPResolution = DbConnectionStringDefaults.TransparentNetworkIPResolution;
         private string _networkLibrary = DbConnectionStringDefaults.NetworkLibrary;
 #else
-        internal const int DeprecatedKeywordsCount = 5;
+        internal const int DeprecatedKeywordsCount = 4;
 #endif
         #endregion //Fields
 
--- /dev/null
+++ b/upstream/src/Microsoft.Data.SqlClient/src/Microsoft/Data/SqlClient/SqlConnectionTcpTransport.cs
@@ -0,0 +1,76 @@
+// GhostShell extension to the pinned MIT-licensed SqlClient source.
+using System;
+using System.Net.Sockets;
+using System.Threading;
+using System.Threading.Tasks;
+using Microsoft.Data.ProviderBase;
+
+namespace Microsoft.Data.SqlClient
+{
+    /// <summary>Owns the physical TCP route and pool identity for a connection.</summary>
+    public sealed class SqlConnectionTcpTransport
+    {
+        private readonly Func<string, int, bool, SqlConnectionIPAddressPreference, CancellationToken, Task<Socket>> _connect;
+
+        /// <summary>The factory must honor cancellation and return a connected socket owned by SqlClient.</summary>
+        public SqlConnectionTcpTransport(
+            Func<string, int, bool, SqlConnectionIPAddressPreference, CancellationToken, Task<Socket>> connect,
+            CancellationToken lifetime)
+        {
+            _connect = connect ?? throw new ArgumentNullException(nameof(connect));
+            Lifetime = lifetime;
+        }
+
+        /// <summary>Cancelling the route invalidates its pooled connections and future opens.</summary>
+        public CancellationToken Lifetime { get; }
+
+        internal Socket Connect(string host, int port, bool parallel, SqlConnectionIPAddressPreference preference, TimeoutTimer timeout)
+        {
+            Lifetime.ThrowIfCancellationRequested();
+            using CancellationTokenSource deadline = CancellationTokenSource.CreateLinkedTokenSource(Lifetime);
+            if (!timeout.IsInfinite)
+            {
+                deadline.CancelAfter(TimeSpan.FromMilliseconds(timeout.MillisecondsRemaining));
+            }
+
+            Task<Socket> pending = _connect(host, port, parallel, preference, deadline.Token)
+                ?? throw new InvalidOperationException("The routed TCP factory returned no operation.");
+            Socket socket;
+            try
+            {
+                socket = pending.WaitAsync(deadline.Token).GetAwaiter().GetResult();
+            }
+            catch
+            {
+                // A factory which completes after the deadline must not leak its socket.
+                _ = DisposeLateSocketAsync(pending);
+                throw;
+            }
+
+            if (socket == null)
+            {
+                throw new InvalidOperationException("The routed TCP factory returned no socket.");
+            }
+            if (deadline.IsCancellationRequested || !socket.Connected)
+            {
+                socket.Dispose();
+                deadline.Token.ThrowIfCancellationRequested();
+                throw new InvalidOperationException("The routed TCP factory returned a disconnected socket.");
+            }
+            return socket;
+        }
+
+        private static async Task DisposeLateSocketAsync(Task<Socket> pending)
+        {
+            try
+            {
+                Socket socket = await pending.ConfigureAwait(false);
+                socket?.Dispose();
+            }
+            catch
+            {
+                // Observe a failed abandoned connect. Its original failure is already returned.
+            }
+        }
+    }
+}
