<Project>
  <!-- Pinned upstream dependency boundary, matching vendor/exclr8cef.
       The RFC 1929 patch is separately recorded and tested by first-party tests. -->
  <PropertyGroup>
    <GhostShellVendoredSourceProject>$([MSBuild]::NormalizePath('$(MSBuildThisFileDirectory)', 'SSH.NET.csproj'))</GhostShellVendoredSourceProject>
  </PropertyGroup>
  <PropertyGroup Condition="'$(MSBuildProjectFullPath)' == '$(GhostShellVendoredSourceProject)'">
    <!-- Share the repository's runtime-lock validation without first-party analyzers. -->
    <GhostShellVendoredLockedProject>true</GhostShellVendoredLockedProject>
    <NuGetLockFilePath Condition="'$(RuntimeIdentifier)' != ''">packages.$(RuntimeIdentifier).lock.json</NuGetLockFilePath>
    <NuGetLockFilePath Condition="'$(RuntimeIdentifier)' == '' and '$(RuntimeIdentifiers)' != ''">packages.$(RuntimeIdentifiers).lock.json</NuGetLockFilePath>
  </PropertyGroup>
</Project>
