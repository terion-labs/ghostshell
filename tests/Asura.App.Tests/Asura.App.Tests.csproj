<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia.Headless" />
    <PackageReference Include="coverlet.collector" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <Compile Include="..\SecurityCampaignCanaries.cs"
             Link="Shared\SecurityCampaignCanaries.cs" />
    <!-- Shared by link rather than copied: a second hand-written terminal fake
         would be a second set of rules to drift from the first, and these two
         files depend on nothing else in their own project. -->
    <Compile Include="..\Asura.SessionHost.Tests\FakeTerminalSession.cs"
             Link="Shared\FakeTerminalSession.cs" />
    <Compile Include="..\Asura.SessionHost.Tests\FakeTerminalSessionFactory.cs"
             Link="Shared\FakeTerminalSessionFactory.cs" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Asura.App\Asura.App.csproj" />
    <!-- The real session host, so a shell-level test can be wrong about the
         host's rules instead of agreeing with a fake that has none. -->
    <ProjectReference Include="..\..\src\Asura.SessionHost\Asura.SessionHost.csproj" />
  </ItemGroup>

</Project>
