<UserControl
    x:Class="Asura.App.Views.Components.SavedConnectionShortcutView"
    x:DataType="vm:SavedConnectionShortcutViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Asura.App.ViewModels"
    xmlns:components="using:Asura.App.Views.Components"
    xmlns:controls="using:Asura.App.Controls"
    xmlns:icons="using:FluentIcons.Avalonia"
    HorizontalContentAlignment="Stretch"
    VerticalContentAlignment="Center">
  <!--
    One saved target, one row. The row opens it the usual way; the chevron
    appears only where the target supports more than one adapter, and offers
    the rest. Listing a host once per adapter instead said the same thing four
    times and buried the ordinary case.
  -->
  <Grid ColumnDefinitions="*,Auto">
    <Button
        Classes="ListRow LauncherListRow"
        Click="OnPrimaryClick"
        HorizontalContentAlignment="Stretch"
        IsEnabled="{Binding CanOpen}"
        ToolTip.Tip="{Binding DefaultLaunch.Label}"
        AutomationProperties.Name="{Binding Name}">
      <!-- Managed where it is used. A saved connection had to be edited from a
           screen of its own, which meant leaving the place you were opening it
           from to change the thing you were opening. -->
      <Button.ContextMenu>
        <ContextMenu IsVisible="{Binding CanEdit}">
          <MenuItem Header="Edit connection…" Click="OnEditClick" />
          <MenuItem Header="Delete connection" Click="OnDeleteClick" />
        </ContextMenu>
      </Button.ContextMenu>
      <controls:ListItem
          ContentPadding="{controls:Inset Horizontal=Md, Vertical=Sm}"
          Title="{Binding Name}"
          Detail="{Binding Detail}">
        <controls:ListItem.Leading>
          <Border VerticalAlignment="Center" Classes="StatusDot" Classes.available="{Binding CanOpen}" />
        </controls:ListItem.Leading>
        <controls:ListItem.Trailing>
          <components:CountPill Value="{Binding Kind}" />
        </controls:ListItem.Trailing>
      </controls:ListItem>
    </Button>
    <Button
        x:Name="ShortcutMenuButton"
        Grid.Column="1"
        Classes="IconButton"
        IsEnabled="{Binding CanOpen}"
        IsVisible="{Binding HasAlternatives}"
        ToolTip.Tip="Open with"
        AutomationProperties.Name="{Binding Name, StringFormat={}More ways to open {0}}">
      <icons:SymbolIcon Symbol="ChevronDown" FontSize="12" />
      <Button.Flyout>
        <Flyout Placement="Bottom" FlyoutPresenterClasses="SavedConnectionMenu">
          <ItemsControl ItemsSource="{Binding AlternativeLaunches}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button
                    Classes="FlyoutMenuItem"
                    Click="OnAlternativeClick"
                    AutomationProperties.Name="{Binding Label}">
                  <StackPanel Orientation="Horizontal" Spacing="{DynamicResource ShellSpaceSm}">
                    <icons:SymbolIcon VerticalAlignment="Center" Symbol="{Binding Icon}" FontSize="14" />
                    <TextBlock VerticalAlignment="Center" Text="{Binding Label}" />
                  </StackPanel>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </Flyout>
      </Button.Flyout>
    </Button>
  </Grid>
</UserControl>
